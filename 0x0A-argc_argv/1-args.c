#include <stdio.h>
#include "main.h"

/**
 * main -  prints the number of arguments passed into it
 * @argc: input value
 * @argv: input value
 * Return: 0 Always
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
